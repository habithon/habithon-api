{
  "author": "Habithon Group",
  "license": "ISC",
  "description": "LAP 2 project",
  "scripts": {
    "start": "nodemon -L index.js",
    "test": "jest --setupFiles ./tests/integration/config.js --watchAll --verbose --detectOpenHandles --coverage",
    "unitTests": "jest --testPathPattern=/unit/ --watchAll --verbose",
    "coverage": "jest --testPathPattern=/unit/ --coverage"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "supertest": "^6.2.4"
  }
}
